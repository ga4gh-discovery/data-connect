{
  "name": "ga4gh-discovery-search-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.14"
  },
  "private": true,
  "scripts": {
    "build": "openapi bundle -o dist/dist.yaml && gh-openapi-docs",
    "test": "openapi lint"
  }
}
